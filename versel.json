{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "includeFiles": ["!**/tests/**"]
        }
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/index.js" }
    ]
  }
  